name: Upgrade Farmandeh Platform v0.4

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm v0.4 workflow
        run: echo "Farmandeh v0.4 workflow is ready"
