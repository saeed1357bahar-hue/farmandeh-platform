# Runtime v0.3 checkpoint

Foundation v0.2 remains the architectural base. Runtime v0.3 is the first executable checkpoint.

The web layer is intentionally dependency-free so it can be tested immediately before introducing a framework, backend, or Android wrapper.
